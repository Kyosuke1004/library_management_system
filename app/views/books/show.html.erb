<h1 class="title is-4 mb-4">本の詳細</h1>
<div class="box">
  <p class="title is-4"><%= @book.title %></p>
  <p class="subtitle is-6">著者: <%= @book.authors.map(&:name).join(', ') %></p>
  <hr>
  <p>出版社: <%= @book.publisher %></p>
  <p>出版年: <%= @book.published_year %></p>
  <p>ISBN: <%= @book.isbn %></p>
  <p><strong>タグ:</strong>
    <% if @book.tags.any? %>
      <%= @book.tags.map(&:name).join(', ') %>
    <% else %>
      <span class="has-text-grey">タグなし</span>
    <% end %>
  </p>
  <p>
    <strong>状態:</strong>
    <% if @book.available? %>
      貸出可能
    <% else %>
      貸出中
    <% end %>
  </p>
  <p><strong>在庫数:</strong> <%= @book.book_items.count %> 冊</p>
  <p><strong>在庫一覧:</strong></p>
  <ul>
    <% @book.book_items.each do |item| %>
      <li class="is-flex is-align-items-center" style="gap: 10px;">
        <span>在庫ID: <%= item.id %></span>
        <% if item.loans.currently_borrowed.any? %>
          <% current_loan = item.loans.currently_borrowed.first %>
          <span class="has-text-danger">貸出中</span>
          <% if user_signed_in? %>
            <% if current_user.admin? && current_loan.user != current_user %>
              <!-- 管理者による強制返却 -->
              <span>（<%= current_loan.user.email %>）</span>
              <%= button_to '強制返却', loan_path(current_loan), method: :patch,
                params: { force: true },
                class: "button is-danger is-light is-small",
                data: { turbo_confirm: '本当に強制返却しますか？' } %>
            <% elsif current_loan.user == current_user %>
              <!-- 自分の返却 -->
              <%= button_to '返却', loan_path(current_loan), method: :patch,
                class: "button is-link is-light is-small" %>
            <% end %>
          <% end %>
        <% else %>
          <% if user_signed_in? %>
            <!-- ログインしている人だけ貸出可能 -->
            <%= button_to '貸出', loans_path(book_item_id: item.id), method: :post,
              class: "button is-primary is-light is-small" %>
          <% end %>
        <% end %>
      </li>
    <% end %>
  </ul>
  <div class="buttons mt-4">
    <%= link_to '一覧に戻る', books_path, class: "button is-light" %>
    <% if current_user&.admin? %>
      <%= link_to '編集', edit_book_path(@book), class: "button is-warning is-light" %>
      <%= link_to '削除', book_path(@book), data: { turbo_method: :delete, turbo_confirm: '本当に削除しますか？' }, class: "button is-danger is-light" %>
    <% end %>
  </div>
</div>
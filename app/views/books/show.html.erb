<h1 class="title is-4 mb-4">本の詳細</h1>
<div class="box">
  <p class="title is-4"><%= @book.title %></p>
  <p class="subtitle is-6">著者: <%= @book.authors.map(&:name).join(', ') %></p>
  <hr>
  <p>出版社: <%= @book.publisher %></p>
  <p>出版年: <%= @book.published_year %></p>
  <p>ISBN: <%= @book.isbn %></p>
  <p><strong>在庫数:</strong> <%= @book.book_items.count %> 冊</p>
  <p>
    <strong>状態:</strong>
    <% if @book.available? %>
      貸出可能
    <% else %>
      貸出中
    <% end %>
  </p>
  <div class="buttons mt-4">
    <% if user_signed_in? %>
      <% if @book.available? %>
        <%= button_to '貸出', loans_path(book_id: @book.id), method: :post, class: "button is-primary is-light" %>
      <% else %>
        <% current_loan = @book.loans.currently_borrowed.first %>
        <% if current_loan&.user == current_user %>
          <%= button_to '返却', loan_path(current_loan), method: :patch, class: "button is-link is-light" %>
        <% end %>
      <% end %>
    <% end %>
    <%= link_to '一覧に戻る', books_path, class: "button is-light" %>
    <% if current_user&.admin? %>
      <%= link_to '編集', edit_book_path(@book), class: "button is-warning is-light" %>
      <%= link_to '削除', book_path(@book), data: { turbo_method: :delete, turbo_confirm: '本当に削除しますか？' }, class: "button is-danger is-light" %>
    <% end %>
  </div>
</div>